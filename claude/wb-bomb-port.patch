--- /tmp/gs-stack-wholesale.css	2026-09-10 00:45:10.182439100 -0400
+++ src/components/GameScreen.css	2026-09-10 00:45:10.238283100 -0400
@@ -152,7 +152,7 @@
   .game-stage--wb .game-used { grid-area: used; align-self: center; width: 100%; margin: 0; }
   .game-stage--wb .game-input-row { grid-area: input; align-self: start; width: 100%; margin: 0 auto; }
   /* Bigger hero bomb now that it owns a dedicated column. */
-  .game-stage--wb .bomb-svg { width: clamp(150px, 26vh, 240px); height: auto; }
+  .game-stage--wb .bomb-svg { width: clamp(150px, 30vh, 250px); height: auto; }
 }
 
 /* Short wide windows: compress vertical rhythm + bomb so the broadcast grid still fits
@@ -163,7 +163,7 @@
     padding-top: clamp(10px, 1.6vh, 20px);
     padding-bottom: clamp(10px, 1.6vh, 20px);
   }
-  .game-stage--wb .bomb-svg { width: clamp(104px, 20vh, 180px); }
+  .game-stage--wb .bomb-svg { width: clamp(150px, 30vh, 250px); }
   .game-stage--wb .game-combo { font-size: clamp(30px, 6.2vh, 54px); }
   .game-stage--wb .game-combo-box { padding: clamp(8px, 1.6vh, 20px) 16px; }
 }
