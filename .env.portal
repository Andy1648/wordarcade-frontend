# Loaded by Vite ONLY for `--mode portal` (npm run build:portal). Flags the
# isolated portal/iframe build so App.jsx skips the intro chain and lands on the
# menu. The default `vite build` (production mode) never reads this file.
VITE_PORTAL=1
